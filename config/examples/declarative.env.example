# Declarative / low-code UI authoring
# Keep cross-origin navigation disabled unless the target origin has been explicitly reviewed.
DECLARATIVE_ALLOW_EXTERNAL_NAVIGATION=false

# Optional: run one specific scenario file through the dynamic declarative spec.
# SCENARIO_FILE=projects/demo/data/scenarios/todo-smoke.yaml

import type { BaseApiClient } from '../../../../src/framework/api/base-api.client';

/** Project API entrypoint; add domain services here when API coverage is introduced. */
export class SdetPracticeApiFacade {
  constructor(_client: BaseApiClient) {}
}

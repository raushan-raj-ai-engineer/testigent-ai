import type { BenchmarkIntelligenceSummary } from '../benchmark/benchmark-analyzer.js';

/** Standalone comparative/scale benchmark evidence page with explicit claim boundaries. */
export function renderBenchmarkIntelligenceHtml(summary: BenchmarkIntelligenceSummary): string {
  const comparativeRows = summary.comparative.metrics.map(metric => `<tr><td>${escapeHtml(metric.metric)}</td><td>${escapeHtml(metric.datasetVersion)}</td><td>${escapeHtml(metric.baselineLabel)}</td><td>${metric.samples}</td><td>${escapeHtml(metric.applications.join(', ') || '—')}</td><td>${metric.baselineMedian} ${escapeHtml(metric.unit)}</td><td>${metric.testigentMedian} ${escapeHtml(metric.unit)}</td><td>${metric.deltaPercent}%</td><td><span class="state ${stateClass(metric.result)}">${escapeHtml(metric.result)}</span></td></tr>`).join('');
  const scaleRows = summary.scale.map(item => `<tr><td>${escapeHtml(item.id)}</td><td>${escapeHtml(item.application)}</td><td>${escapeHtml(item.datasetVersion)}</td><td>${item.result.caseCount}</td><td>${item.result.workers}</td><td>${item.result.shards}</td><td>${item.result.wallClockMs} ms</td><td>${item.result.shardImbalancePercent === null ? '—' : `${item.result.shardImbalancePercent}%`}</td><td><span class="state ${stateClass(item.result.status)}">${escapeHtml(item.result.status)}</span></td></tr>`).join('');
  const falseHealRows = summary.falseHeal.map(item => `<tr><td>${escapeHtml(item.id)}</td><td>${escapeHtml(item.application)}</td><td>${escapeHtml(item.datasetVersion)}</td><td>${item.result.injectedBusinessFailures}</td><td>${item.result.falseHeals}</td><td>${item.result.falseHealRatePercent === null ? '—' : `${item.result.falseHealRatePercent}%`}</td><td><span class="state ${stateClass(item.result.status)}">${escapeHtml(item.result.status)}</span></td></tr>`).join('');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>TestigentAI Benchmark Intelligence</title><style>${styles()}</style></head><body><main><a href="./index.html">← Business dashboard</a><header><div><div class="eyebrow">TESTIGENTAI · BENCHMARK INTELLIGENCE</div><h1>Evidence-based Differentiation</h1><p>Versioned baseline comparisons and measured scale evidence. Synthetic planning diagnostics are never represented as browser-scale certification.</p></div><span class="state ${stateClass(summary.comparative.differentiationStatus)}">${escapeHtml(summary.comparative.differentiationStatus)}</span></header><section class="cards"><article><span>Applications</span><b>${summary.comparative.applications.length}</b></article><article><span>Comparative groups</span><b>${summary.comparative.metrics.length}</b></article><article><span>Scale passes</span><b>${summary.scalePasses}</b></article><article><span>False-heal passes</span><b>${summary.falseHealPasses}</b></article></section><section class="panel"><h2>Plain-baseline comparison</h2>${comparativeRows ? `<div class="table"><table><thead><tr><th>Metric</th><th>Dataset</th><th>Baseline</th><th>Samples</th><th>Applications</th><th>Baseline median</th><th>TestigentAI median</th><th>Delta</th><th>Evidence status</th></tr></thead><tbody>${comparativeRows}</tbody></table></div>` : '<div class="empty">No baseline-backed benchmark samples recorded yet.</div>'}</section><section class="panel"><h2>False-heal safety evidence</h2>${falseHealRows ? `<div class="table"><table><thead><tr><th>Evidence</th><th>Application</th><th>Dataset</th><th>Injected failures</th><th>False heals</th><th>Rate</th><th>Status</th></tr></thead><tbody>${falseHealRows}</tbody></table></div>` : '<div class="empty">No persisted false-heal benchmark evidence recorded yet.</div>'}</section><section class="panel"><h2>Scale certification evidence</h2>${scaleRows ? `<div class="table"><table><thead><tr><th>Evidence</th><th>Application</th><th>Dataset</th><th>Cases</th><th>Workers</th><th>Shards</th><th>Wall clock</th><th>Imbalance</th><th>Status</th></tr></thead><tbody>${scaleRows}</tbody></table></div>` : '<div class="empty">No measured scale evidence recorded yet.</div>'}</section><section class="trust"><b>Truth boundary:</b> ${escapeHtml(summary.comparative.truthBoundary)}</section></main></body></html>`;
}
function stateClass(value: string): string { return value === 'PASS' || value === 'IMPROVED' || value === 'EVIDENCE_READY' ? 'ok' : value === 'FAIL' || value === 'REGRESSED' ? 'bad' : 'warn'; }
function escapeHtml(value: unknown): string { return String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char] ?? char)); }
function styles(): string { return `:root{--bg:#f4f7fb;--panel:#fff;--ink:#0f172a;--muted:#64748b;--line:#dfe7f1}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:1400px;margin:auto;padding:24px}a{color:#2563eb}header{display:flex;justify-content:space-between;gap:20px;background:#172554;color:#fff;padding:24px;border-radius:18px;margin-top:14px}header h1{margin:4px 0 8px}.eyebrow{font-size:10px;letter-spacing:.14em;color:#bfdbfe;font-weight:800}header p{color:#dbeafe;margin:0}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:14px 0}.cards article,.panel,.trust{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:16px}.cards span{font-size:10px;text-transform:uppercase;color:var(--muted);font-weight:800}.cards b{display:block;font-size:25px;margin-top:5px}.panel{margin-top:14px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:980px}th,td{text-align:left;padding:10px;border-bottom:1px solid #eef2f7;font-size:12px}th{font-size:10px;text-transform:uppercase;color:var(--muted)}.state{display:inline-block;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:900}.state.ok{background:#d1fae5;color:#065f46}.state.bad{background:#fee2e2;color:#991b1b}.state.warn{background:#fef3c7;color:#92400e}.trust{margin-top:14px;background:#eff6ff;border-left:4px solid #3b82f6;font-size:12px;line-height:1.5}.empty{padding:26px;text-align:center;color:var(--muted)}@media(max-width:850px){.cards{grid-template-columns:repeat(2,1fr)}}`; }

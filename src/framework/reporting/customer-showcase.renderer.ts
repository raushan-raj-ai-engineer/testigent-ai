import type { FailureIntelligenceSummary } from '../failure-intelligence/failure-intelligence.types.js';
import type { ShowcaseDataset } from '../failure-intelligence/showcase-policy.js';

/** Renders the isolated one-click customer showcase; every metric is visibly synthetic and non-claimable. */
export function renderCustomerShowcaseHtml(dataset: ShowcaseDataset, failure: FailureIntelligenceSummary): string {
  const categories = Object.entries(failure.categories).filter(([, count]) => count > 0).map(([name, count]) => `<div><span>${escapeHtml(name.replaceAll('_', ' '))}</span><b>${count}</b></div>`).join('');
  const incidentRows = failure.clusters.map(item => `<tr><td><code>${escapeHtml(item.fingerprint)}</code></td><td>${escapeHtml(item.category)}</td><td>${item.affectedScenarios}</td><td>${escapeHtml(item.applications.join(', '))}</td><td>${escapeHtml(item.confidence)}</td></tr>`).join('');
  const scenarios = failure.classifications.map(item => `<tr><td>${escapeHtml(item.scenarioId)}</td><td>${escapeHtml(item.title)}</td><td>${escapeHtml(item.category)}</td><td>${escapeHtml(item.confidence)}</td><td>${escapeHtml(item.rationale)}</td></tr>`).join('');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>TestigentAI Customer Showcase</title><style>${styles()}</style></head><body><main><div class="showcase"><div><b>SHOWCASE MODE · SYNTHETIC DATA</b><p>This page demonstrates product behavior only. Nothing here is customer, production, benchmark, scale-certification or release evidence.</p></div><span>claimEligible = false</span></div><header><div><div class="eyebrow">TESTIGENTAI v1.9 · CUSTOMER SHOWCASE</div><h1>${escapeHtml(dataset.title)}</h1><p>One-click view of failure intelligence, product evidence, healing, contract, adoption, scale and agentic signals.</p></div><a href="./failure-intelligence.html">Open failure drill-down →</a></header><section class="grid hero-grid"><article><span>Example failed scenarios</span><b>${failure.analyzedScenarios}</b><small>realistic synthetic examples</small></article><article><span>Unique incidents</span><b>${failure.uniqueIncidents}</b><small>fingerprint compression</small></article><article><span>Applications represented</span><b>${dataset.adoption.applications}</b><small>showcase portfolio</small></article><article><span>Report completeness</span><b>${dataset.benchmark.reportCompletenessPercent}%</b><small>demonstration metric</small></article></section><section class="panel"><div class="head"><div><h2>Failure Intelligence</h2><p>Evidence-first classification before optional AI. Repeated failures collapse into common incidents.</p></div><a href="./failure-intelligence.html">Detailed evidence →</a></div><div class="category-grid">${categories}</div><div class="table"><table><thead><tr><th>Fingerprint</th><th>Category</th><th>Affected scenarios</th><th>Applications</th><th>Confidence</th></tr></thead><tbody>${incidentRows}</tbody></table></div></section><section class="grid"><article><div class="section-label">ADOPTION</div><h3>Pilot Intelligence</h3><p><b>${dataset.adoption.applications}</b> apps · <b>${dataset.adoption.contributors}</b> contributors · <b>${dataset.adoption.observations}</b> observations</p><p>Time to first passing test: <b>${dataset.adoption.timeToFirstPassingTestMinutes} min</b></p><p>Requirement → approved test: <b>${dataset.adoption.requirementToApprovedTestMinutes} min</b></p><p>Median triage: <b>${dataset.adoption.triageMedianMinutes} min</b></p></article><article><div class="section-label">BENCHMARK</div><h3>Plain baseline comparison</h3><p>Baseline: <code>${escapeHtml(dataset.benchmark.baseline)}</code></p><p>Authoring-time delta: <b>${dataset.benchmark.authoringTimeImprovementPercent}%</b></p><p>Triage-time delta: <b>${dataset.benchmark.triageTimeImprovementPercent}%</b></p><p class="note">Synthetic showcase values are never differentiation claims.</p></article><article><div class="section-label">SCALE</div><h3>Execution planning</h3><p>Planned cases: <b>${dataset.scale.plannedCases.toLocaleString()}</b></p><p>Shards: <b>${dataset.scale.shards}</b> · workers: <b>${dataset.scale.workers}</b></p><p class="note">Measured certification: <b>NO</b>. This is planning visualization only.</p></article><article><div class="section-label">API CONTRACT</div><h3>OpenAPI Intelligence</h3><p>Operations checked: <b>${dataset.apiContract.operationsChecked}</b></p><p>Breaking changes: <b>${dataset.apiContract.breakingChanges}</b></p><p>Response validation failures: <b>${dataset.apiContract.validationFailures}</b></p></article><article><div class="section-label">HEALING</div><h3>Safe recovery</h3><p>Validated recoveries: <b>${dataset.healing.validatedRecoveries}</b></p><p>Unsafe candidates rejected: <b>${dataset.healing.rejectedUnsafeCandidates}</b></p><p>False heals: <b>${dataset.healing.falseHeals}</b></p><p class="note">Only validated semantic recovery counts as healing.</p></article><article><div class="section-label">AGENTIC / MCP</div><h3>Governed assistance</h3><p>Planner decisions: <b>${dataset.agentic.plannerDecisions}</b></p><p>Generation proposals: <b>${dataset.agentic.generationProposals}</b></p><p>Reviewer rejections: <b>${dataset.agentic.reviewerRejections}</b></p><p>MCP read-only calls: <b>${dataset.agentic.mcpReadOnlyCalls}</b></p></article></section><section class="panel"><h2>10 realistic examples</h2><div class="table"><table><thead><tr><th>Scenario</th><th>Business intent</th><th>Classification</th><th>Confidence</th><th>Why</th></tr></thead><tbody>${scenarios}</tbody></table></div></section><section class="freeze"><b>Reporting freeze boundary</b><p>v1.9 is the planned reporting-surface freeze. Future releases may fix correctness, security, accessibility, compatibility and performance, but should not add new dashboard KPIs/cards without an explicit reporting architecture review.</p></section></main></body></html>`;
}

function escapeHtml(value: unknown): string { return String(value ?? '').replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char] ?? char)); }
function styles(): string { return `:root{--bg:#eef3f9;--panel:#fff;--ink:#0f172a;--muted:#64748b;--line:#dbe5ef;--navy:#172554}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:1460px;margin:auto;padding:24px}.showcase{display:flex;justify-content:space-between;gap:20px;align-items:center;background:#fff7ed;border:1px solid #fdba74;border-left:6px solid #f97316;border-radius:14px;padding:14px 16px;margin-bottom:14px}.showcase b{color:#9a3412}.showcase p{margin:4px 0 0;color:#7c2d12;font-size:12px}.showcase span{font-family:ui-monospace,monospace;font-size:11px;color:#9a3412;background:#ffedd5;padding:7px 9px;border-radius:8px}header{background:linear-gradient(135deg,#172554,#1e3a8a);color:#fff;border-radius:20px;padding:28px;display:flex;justify-content:space-between;gap:24px;align-items:center}header h1{margin:5px 0 8px;font-size:32px}.eyebrow,.section-label{font-size:10px;letter-spacing:.14em;font-weight:900;color:#93c5fd}header p{margin:0;color:#dbeafe}header a{color:#172554;background:#fff;padding:11px 14px;border-radius:10px;text-decoration:none;font-weight:800;white-space:nowrap}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:14px}.hero-grid{grid-template-columns:repeat(4,1fr)}article,.panel,.freeze{background:var(--panel);border:1px solid var(--line);border-radius:15px;padding:17px}.hero-grid article span{font-size:10px;text-transform:uppercase;color:var(--muted);font-weight:800}.hero-grid article b{display:block;font-size:27px;margin:5px 0}.hero-grid article small{color:var(--muted)}article h3{margin:6px 0 12px}.section-label{color:#2563eb}article p{font-size:12px;line-height:1.5;margin:7px 0}.note{color:var(--muted)}.panel{margin-top:14px}.head{display:flex;justify-content:space-between;gap:16px;align-items:start}.head h2,.panel h2{margin-top:0}.head p{margin:4px 0;color:var(--muted);font-size:12px}.head a{color:#2563eb}.category-grid{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:14px 0}.category-grid div{background:#f8fafc;border:1px solid #e2e8f0;border-radius:11px;padding:11px}.category-grid span{font-size:9px;color:var(--muted);font-weight:800}.category-grid b{display:block;font-size:20px;margin-top:3px}.table{overflow:auto}table{width:100%;border-collapse:collapse;min-width:900px}th,td{text-align:left;vertical-align:top;padding:10px;border-bottom:1px solid #edf2f7;font-size:12px}th{font-size:10px;color:var(--muted);text-transform:uppercase}.freeze{margin-top:14px;background:#eff6ff;border-left:5px solid #3b82f6}.freeze p{font-size:12px;line-height:1.55;margin-bottom:0}@media(max-width:960px){.grid,.hero-grid{grid-template-columns:repeat(2,1fr)}.category-grid{grid-template-columns:repeat(3,1fr)}header,.showcase{align-items:flex-start;flex-direction:column}}`; }

# TestigentAI v1.9.2 — Independent Re-review Closure

## Basis

The v1.9.2 corrective release responds to `docs/reviews/TestigentAI-v1.9.1-Independent-Re-review.md` (bundled document SHA-256 `2918abb5f587f806bf29653c0fe3048d8966dd2d6749892f3e6e36272c354a79`). The reviewer records the reviewed v1.9.1 input ZIP SHA-256 as `5488d96db20a841aaa4eae6f4a5754498c0260886464965ed9268d40bc53bbcf`. The reviewer retained the architecture but reported broader boundary/interaction cases after the original v1.9.1 fixes.

## Closure map

| Review item | Corrective contract in v1.9.2 | Primary acceptance coverage |
| --- | --- | --- |
| R04 P1 project containment | Canonical containment is anchored to the selected project, not merely repository root; escaping project/root/subdirectory/leaf symlinks are rejected; future-target ancestors and promotion paths are revalidated. | v1.9.2 closure tests + direct project-symlink probe |
| R01 P1 response/schema validation | Boolean true/false schemas are first-class; enum applies to null; `$ref` siblings are conjunctive; unsupported forms fail closed rather than return success. | public response-validator cases in v1.9.2 closure suite + direct probe |
| R02 P1 compatibility detection | Query/path/cookie names remain case-sensitive; header names are case-insensitive; supported request restrictions compare directionally; unsupported changed constraint families produce `INCOMPLETE_COMPARISON`. | v1.9.2 directional compatibility cases + direct probe |
| R09 P1 report regeneration | Evidence publication has stable content-aware identities; regenerating the same occurrence reuses links; immutable history remains conflict-detecting; history persistence follows complete bundle publication. | same full bundle generated twice + closure integration cases |
| R07 P2 provenance | Legacy text/category heuristics stay heuristic via `legacyCategoryHint`; the adapter no longer upgrades them to structured evidence fields. | legacy classifier -> adapter -> deterministic classifier cases |
| R10 P2 SQL | PostgreSQL native `$1..$n` is supported, mixed placeholder styles are rejected, JSON operators are distinguished, and SQL Server bracket identifiers are lexical state. | SQL helper + real adapter preparation probes |
| R03 P2 TLS | Strict/protected modes require encryption, SSL mode is validated, and insecure certificate verification overrides are rejected. | policy cases + adapter TLS builder probe |
| R12 P2 MCP transport | Explicit lifecycle requires initialize response then initialized notification; active work receives abort signals; input is byte-capped while frames are assembled before newline; control notifications remain serviceable. | closure suite + real child-process stdio harness |
| R11 viewer qualification | CSV cells are neutralized centrally and qualified through LibreOffice open/save/reopen with formula-node inspection. | `scripts/csv-viewer-qualification.ts` |

## Retained closed controls

The v1.9.1 controls for R05 provenance defaults, R06 complete redaction, R08 cause-aware clustering, R13 incremental ledger/corruption visibility and R14 full-SHA GitHub Action pinning remain in place and stay covered by the v1.9.1 closure suite and existing hardening suites.

## Reporting freeze

No new reporting product surface is introduced. R09 changes are corrective idempotency/integrity fixes required to preserve report regeneration and immutable history semantics.

## Certification status

This document originally recorded corrective closure before certification. v1.9.2 subsequently certified at immutable commit `f061e4ef1fd869888fdae721d4790ce2058070ae` after main CI `34839795426`, fresh main Release Compatibility `34840215663` (5/5), and tag-triggered Release Compatibility `34840673003` (5/5) all passed. See `docs/68-v1.9.2-VALIDATION-HANDOFF.md` for the pre-certification validation boundary and final connected-certification outcome.

# TestigentAI v1.9.0 — Candidate Handoff

## Certified baseline

Apply this candidate only to a clean feature branch created from the current v1.8.0 post-certification main line. The immutable v1.8.0 release tag remains at:

`1ad48d67661df02f51e5b6268a3f0d74bf2a182d`

The current post-release documentation main commit at candidate creation is:

`6090b702a56078a546cfe6dd0d2bc2cbaba92dd6`

Do not move or recreate `v1.8.0`.

## Candidate scope

- deterministic Failure Intelligence taxonomy;
- stable failure fingerprinting and incident compression;
- fail-honest unknown classification;
- live-only immutable history boundary;
- one-click 10-scenario synthetic Customer Showcase;
- normal-dashboard links to live failure intelligence and isolated showcase;
- read-only failure-triage MCP tools;
- npm Bulk Advisory security fallback carried forward;
- reporting-surface freeze policy and executable contracts;
- v1.8/v1.7 historical and certified source protections retained during upgrade.

## Upgrade rule

`APPLY_UPGRADE.sh` deliberately preserves certified/historical v1.7 files that the v1.9 feature does not need to modify. The target repository remains authoritative for those protected paths.

## Candidate rule

This ZIP is a development candidate. Do not create `v1.9.0` merely because local static/offline checks pass. Complete the same connected certification discipline used for v1.8.0.

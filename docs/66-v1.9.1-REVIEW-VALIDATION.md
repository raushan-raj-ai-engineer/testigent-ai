# TestigentAI v1.9.1 — Corrective Hardening Validation

## Purpose

This document records what was actually validated while packaging the v1.9.1 corrective hardening candidate that responds to R01–R14 in the independent v1.9.0 architecture/product review.

Reviewed input archive recorded by the reviewer: `TestigentAI-v1.9.0-REVIEW.zip`, SHA-256 `228fe5a7544a116621a3e679a8b411d1a35e3761c780b5765798a6d62865ccac`.

The immutable certified baseline remains `v1.9.0` at `6a32718353022da4e5ce51dace59e29692340913`. The v1.9.1 package is a corrective review candidate and does not move or recreate that tag.

## Packaging-environment results

The packaging environment uses Node 22.16.0. Registry/DNS access was unavailable, so a clean locked dependency installation could not complete. The package therefore does not claim a fresh connected Playwright/browser or live database run from the packaging environment.

The following dependency-independent validation completed after the final source edits:

- GitHub Action SHA pin policy: PASS;
- release static contract: PASS;
- LF/CRLF portability contract: PASS;
- offline release inventory: PASS;
- Bash syntax for `APPLY_UPGRADE.sh` and `VERIFY_UPGRADE.sh`: PASS;
- all repository TypeScript source/test files transpile with 0 syntax diagnostics;
- reusable-export JSDoc audit: 279 declarations, 0 issues;
- R01–R14 executable counterexample probe against transpiled framework modules: 14/14 PASS;
- R13 crash-window JSONL replay/repair probe: PASS;
- semantic TypeScript check of the hardening-modified scope: 0 product-code errors after excluding only missing external package declarations (`yaml` and `@playwright/test`) caused by the unavailable install.

## Review closure evidence

The executable Playwright regression contract is `tests/framework/v1.9.1-review-closure-contract.spec.ts`. It maps one test to each review finding R01–R14 and exercises the public boundary or production helper involved in the reproduced defect.

The dependency-independent packaging probe executes the same core counterexamples directly against transpiled production modules. It is supplemental evidence, not a replacement for the supported connected test suite.

## Connected certification required before release

From a clean branch based on current post-v1.9.0 `main`, run on supported Node 22:

```bash
npm ci
npm run typecheck
npm run test:review:closure
npm run release:static
npm run showcase:validate
npm run validate:final
npm run security:check
```

For database capability rollout, exercise PostgreSQL, SQL Server and MySQL against trusted TLS endpoints and negative certificate cases. For Root-Cause Intelligence product claims, evaluate a held-out human-labelled corpus and publish category precision, confusion matrix and abstention rate. Those external measurements must not be fabricated by the framework.

Then use the same connected release discipline as v1.9.0: PR CI + rerun, main CI + rerun, fresh five-job Release Compatibility, exact-SHA proof, annotated `v1.9.1` tag, tag-triggered five-job compatibility, and GitHub Release.

## Scope rule

The v1.9 reporting surface remains feature-frozen. This candidate contains corrective correctness/security/compatibility/operational hardening only. No new reporting KPI/card/tab expansion is part of the review closure.
